# YARA Rule Builder

<!--
TASK R1 — write this properly once Milestone 1 works, not before.

A README written against a working app is honest. One written against an
empty repo is a wish list, and reviewers can tell the difference.

When you get there, cover:
  - One line on what it does, then a SCREENSHOT (or a short GIF of an
    error appearing in the gutter as you type). Screenshot first, prose
    second — it's what makes someone read the rest.
  - Why it exists: writing YARA by hand means a compile/fix loop in a
    terminal. This closes that loop in the editor.
  - Run it: docker one-liner, then local dev (two terminals).
  - Architecture: three sentences plus the compile-request path
    (editor -> debounce -> POST /api/compile -> yara-x -> RuleError ->
    gutter). That path IS the project; make it easy to follow.
  - Security notes: untrusted input, size cap, non-root container, no
    persistence. Short section, but include it — it's a security tool,
    and showing you thought about your own attack surface is the point.
  - Honest limitations and what's deliberately out of scope.
  - Authorship: what you wrote, what was scaffolded, what you learned.
    Say it plainly. It reads as confidence, not as a caveat.
-->

Stub. See [TASKS.md](TASKS.md) for the build order and per-file tasks.
